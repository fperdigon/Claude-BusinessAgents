.DS_Store
__pycache__/
*.pyc
.env
.idea/
.vscode/
.claude/settings.local.json

# Private startup data — each founder fills these in via /BussinesAgents:founder
memory/*.md

# All generated outputs — user content, not source
outputs/

# Local MCP server config — machine-specific paths, not portable
.mcp.json

# Git worktrees
.worktrees/

# Superpowers plugin runtime state
.superpowers/
